schema_version: "1.0"
policies:
  - path: agents/single-agent.yaml
  - path: agents/concurrent-agent.yaml
metadata:
  environment: test
  compiled_by: policy-checker-benchmark
  bundle_revision: 1
  policy_revision: benchmark-v1
