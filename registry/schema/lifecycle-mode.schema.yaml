$schema: "https://json-schema.org/draft/2020-12/schema"
title: Lifecycle Mode Contract
type: object
required:
  - mode
  - role_name
  - mission
  - allowed_inputs
  - required_outputs
  - hard_rules
  - failure_conditions
  - evidence_requirements
  - never_do
  - valid_handoff_targets
properties:
  mode:
    $ref: "#/$defs/lifecycle_mode"
  role_name:
    type: string
    minLength: 1
  mission:
    type: string
    minLength: 1
  allowed_inputs:
    type: array
    minItems: 1
    items:
      type: string
      minLength: 1
  required_outputs:
    type: array
    minItems: 1
    items:
      type: string
      minLength: 1
  hard_rules:
    type: array
    minItems: 1
    items:
      type: string
      minLength: 1
  failure_conditions:
    type: array
    minItems: 1
    items:
      type: string
      minLength: 1
  evidence_requirements:
    type: array
    minItems: 1
    items:
      type: string
      minLength: 1
  never_do:
    type: array
    minItems: 1
    items:
      type: string
      minLength: 1
  valid_handoff_targets:
    type: array
    minItems: 1
    items:
      $ref: "#/$defs/lifecycle_mode"
additionalProperties: false
$defs:
  lifecycle_mode:
    type: string
    enum:
      - prototyper
      - builder
      - sweeper
      - grower
      - maintainer

